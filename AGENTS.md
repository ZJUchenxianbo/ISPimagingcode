# AGENTS.md

本文件是 `pyproj` 项目的协作约定。参与本项目的模型在修改代码前应先阅读并遵守这些规则。

## 项目定位

本项目用于反散射问题的数值实验，主题包括 Helmholtz 方程、有限孔径数据、直接采样/正交采样、点散射体、小障碍物成像、以及相关神经网络后处理模块。

代码以可读、可复现实验脚本为主，不追求复杂工程框架。新增实现应尽量贴近已有脚本风格。

## Python 环境

- 项目使用 `.venv/` 虚拟环境（Python 3.14），所有 Python 命令必须通过 `.venv/bin/python` 执行。
- 运行或检查前先确认使用的是虚拟环境而非全局 Python。

## 沟通与代码风格

- 默认用中文说明修改思路、数学含义和验证结果。
- 保持代码清晰显式，优先可读性，不写过度压缩的 Python。
- 变量名尽量保留数学和物理含义，例如 `k`、`alpha`、`obs_angles`、`incident_angles`、`farfield`、`indicator`。
- 优先复用已有工具函数，包括 `scattering_common`（常量/噪声）、`sampling_imaging`（成像指标/孔径）、`target_cases`（几何/绘图）、`forward_scattering`（前向求解）、`obstacle_reconstruction`（重建算法）。
- 重构应以消除重复、打破循环依赖或提取共享逻辑为目的；重构前应先说明理由并取得确认。

## 数学与数值实现

- 修改算法前，应区分这次变化属于数学模型、离散化方法，还是纯代码实现。
- 涉及边界积分、远场模式、采样指标、噪声模型、有限孔径权重时，应在注释或说明中写清对应公式含义。
- 数组形状要保持明确，例如远场矩阵通常为 `(len(obs_angles), len(incident_angles))`。
- 输出习惯保持简单稳定：图片用 `.png`，数组用 `.npz`，实验元数据用 `metadata.json`。

## 代码组织与命名

- 优先减少重复代码，把有限孔径、直接采样、前向散射、目标案例和绘图等可复用逻辑逐步抽到共享模块。
- 实验脚本应主要负责组织参数、调用公共函数、保存结果；不要长期承载可复用的核心算法。
- 公共模块命名应描述数学或数值职责，例如 `scattering_common.py`、`sampling_imaging.py`、`target_cases.py`。
- 实验脚本命名应描述研究对象和任务，例如 `obstacle_direct_imaging.py`；不必为了统一而添加 `experiment_` 前缀。
- 文件名不要包含只需改运行参数就能改变的条件，例如目标个数、噪声水平、孔径大小或网格尺寸。
- 如果几个脚本只是实验条件不同，应优先把条件做成运行时输入并合并入口；如果研究对象或代码逻辑明显不同，不要为了统一命名而强行合并。
- 避免为了改名新增只转发 `main()` 的薄包装文件；若确实需要改名，优先在代码稳定后做真实重命名，并同步更新 import 和 README。
- 公共模块数量要克制，只抽取能明显减少重复或表达清楚数学职责的部分。
- 整理顺序优先为：先抽公共函数，再更新 README，最后在公共模块稳定后考虑文件改名。
- 模块依赖按层级组织：`scattering_common`（Layer 0 根）→ 四大基石（Layer 1：`forward_scattering`、`sampling_imaging`、`target_cases`、`unet_imaging`）→ 桥接模块（Layer 2：`obstacle_direct_sampling`、`obstacle_reconstruction`）→ 实验脚本（Layer 3）。只允许上层依赖下层。
- Layer 1 基石之间应尽量减少交叉依赖；Layer 3 实验脚本 **禁止互相 import**，需要共享的逻辑必须提取到 Layer 1 或 Layer 2。

## README 维护

- 新增脚本、重要命令行参数、输出目录、实验模块或主要功能时，应同步更新 `README.md`。
- README 内容保持简洁，只写用途和必要运行说明，不写复杂长文档。
- 如果只是修 bug 或改内部实现，且用户使用方式没有变化，可以不更新 README。

## AGENTS.md 维护

- 本文件是持续维护的项目约定文件，不是一次性说明。
- 当用户提出新的长期偏好、项目规则、运行习惯或代码组织约定时，应判断是否适合写入本文件。
- 若适合写入，应先询问用户是否加入；只有用户确认后才修改 `AGENTS.md`。
- 临时实验参数、一次性 bug 处理方式或短期安排通常不写入，除非它们会反复影响后续开发。
- 更新本文件时保持简洁，不把它扩展成冗长说明书。
- 维护约定不只是做加法：发现过时、不准确或与当前代码实际不符的约定时，应主动报告并建议修改或删除。

## 神经网络模块

- 神经网络相关模块可以修改和完善，包括数据接口、模型结构、训练参数、保存加载和服务器运行入口。
- 本地验证时不要运行神经网络训练或推理，不触发耗时的 `torch`、GPU 或大规模数据生成流程。
- 本地只做轻量检查，例如 `.venv/bin/python -m py_compile`，或运行明确跳过神经网络的非深度学习路径。
- 如果脚本涉及神经网络，应保留或添加清晰开关，例如 `--no-unet`、`--skip-train`、`--train-unet`，使本地轻量验证和服务器训练路径分离。
- 不要因为本地不运行神经网络就删除或弱化神经网络模块。

## 本地验证

- 优先运行轻量验证：`.venv/bin/python -m py_compile` 语法检查、小网格非神经网络实验、或只生成少量输出的 smoke test。
- 避免默认运行耗时实验；如必须运行，应先缩小网格、边界点数和样本数。
- 本地测试产生的临时输出目录或文件应在验证后删除，不把 smoke test 产物留在项目目录。
- 验证结果应说明运行了什么命令，以及是否跳过了神经网络路径。

## 参考资料

- 可以参考本地文献和代码目录，例如 `../反散射相关书籍和论文/`、`../有限孔径/`、`../有限孔径2/`。
- 如需参考 GitHub 代码，应先说明参考目的；实现时应融入本项目风格，不直接堆叠外部代码。
- 不要虚构论文、定理或代码来源。

## Git 与文件修改

- 修改前先阅读相关文件和现有实现。
- 不回滚用户已有修改，不改无关文件。
- 若工作树已有未提交内容，应只处理当前任务需要的文件。
- 修改代码时同时考虑 README 是否需要更新。
